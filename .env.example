# spaceduck environment configuration
# Copy this to .env and fill in the values

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000
LOG_LEVEL=info   # debug | info | warn | error

# ── Provider ──────────────────────────────────────────────────────────────────
PROVIDER_NAME=gemini                   # gemini | bedrock | openrouter | lmstudio
PROVIDER_MODEL=gemini-2.5-flash        # model name (provider-specific)

# Optional: give the assistant a persona
# SYSTEM_PROMPT=You are spaceduck, a helpful and concise AI assistant.

# ── Google Gemini ─────────────────────────────────────────────────────────────
# Required when PROVIDER_NAME=gemini or EMBEDDING_PROVIDER=gemini
# Get your key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# ── Amazon Bedrock ────────────────────────────────────────────────────────────
# Required when PROVIDER_NAME=bedrock or EMBEDDING_PROVIDER=bedrock
# Generate an API key at: AWS Console → Amazon Bedrock → API keys
# Docs: https://docs.aws.amazon.com/bedrock/latest/userguide/api-keys.html
#
# AWS_BEARER_TOKEN_BEDROCK=bedrock-api-key-...   # paste your Bedrock API key here
# AWS_REGION=us-east-1                           # region where the model is enabled
# PROVIDER_MODEL=us.amazon.nova-2-lite-v1:0      # or us.anthropic.claude-3-5-haiku-20241022-v1:0

# ── OpenRouter ────────────────────────────────────────────────────────────────
# Required when PROVIDER_NAME=openrouter
# Get your key at: https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-...

# ── LM Studio ─────────────────────────────────────────────────────────────────
# Required when PROVIDER_NAME=lmstudio (runs locally, no API key needed by default)
# LMSTUDIO_BASE_URL=http://localhost:1234/v1
# LMSTUDIO_API_KEY=                        # optional

# ── Memory ────────────────────────────────────────────────────────────────────
MEMORY_BACKEND=sqlite
MEMORY_CONNECTION_STRING=spaceduck.db    # file path for SQLite, or :memory: for ephemeral

# ── Embedding / Vector Memory ─────────────────────────────────────────────────
# Enables semantic (cross-lingual) recall across conversations.
# Set EMBEDDING_ENABLED=false to fall back to keyword search only (FTS).
#
# EMBEDDING_ENABLED=true
# EMBEDDING_PROVIDER=bedrock                   # bedrock | gemini | lmstudio
# EMBEDDING_MODEL=amazon.titan-embed-text-v2:0 # Titan V2: 100+ languages incl. Danish
# EMBEDDING_DIMENSIONS=1024                    # Titan V2 supports 256 | 512 | 1024
#
# Gemini alternative:
# EMBEDDING_PROVIDER=gemini
# EMBEDDING_MODEL=text-embedding-004
# EMBEDDING_DIMENSIONS=768
#
# LM Studio alternative:
# EMBEDDING_PROVIDER=lmstudio
# EMBEDDING_MODEL=text-embedding-qwen3-embedding-8b
# EMBEDDING_DIMENSIONS=4096

# ── WhatsApp Channel ──────────────────────────────────────────────────────────
# Enable the WhatsApp channel (uses Baileys / WhatsApp Web protocol).
# On first start, scan the QR code printed in the terminal with your phone.
# WHATSAPP_ENABLED=false
# WHATSAPP_AUTH_DIR=data/whatsapp-auth    # session credentials (gitignored)
