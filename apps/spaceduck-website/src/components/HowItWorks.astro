---
const steps = [
  {
    number: "1",
    title: "Connect any client",
    description:
      "Web UI, Desktop app, CLI, or WhatsApp. All clients connect to the same gateway over HTTP and WebSockets.",
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />`,
  },
  {
    number: "2",
    title: "One gateway",
    description:
      "A local HTTP/WebSocket server with persistent SQLite memory. Facts are extracted, indexed, and recalled across conversations.",
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2m-2-4h.01M17 16h.01" />`,
  },
  {
    number: "3",
    title: "Any provider",
    description:
      "llama.cpp, LM Studio, Bedrock, Gemini, or OpenRouter. Chat and embeddings are independent — mix and match freely.",
    icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />`,
  },
];
---

<section class="bg-slate-50 px-6 py-20 md:py-28">
  <div class="mx-auto max-w-6xl">
    <div class="mx-auto max-w-2xl text-center">
      <h2 class="text-3xl font-bold tracking-tight text-slate-900 md:text-4xl">
        How it works
      </h2>
      <p class="mt-4 text-lg text-text-muted">
        Clients &rarr; Gateway &rarr; Model Providers. That's it.
      </p>
    </div>

    <div class="mt-16 grid gap-8 md:grid-cols-3">
      {
        steps.map((step) => (
          <div class="relative rounded-2xl border border-border bg-white p-8">
            <div class="mb-5 flex items-center gap-3">
              <div class="flex h-10 w-10 items-center justify-center rounded-lg bg-primary/10 text-primary">
                <svg
                  class="h-5 w-5"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                  set:html={step.icon}
                />
              </div>
              <span class="text-sm font-bold text-primary">
                Step {step.number}
              </span>
            </div>
            <h3 class="text-lg font-semibold text-slate-900">{step.title}</h3>
            <p class="mt-2 text-sm leading-relaxed text-text-muted">
              {step.description}
            </p>
          </div>
        ))
      }
    </div>
  </div>
</section>
