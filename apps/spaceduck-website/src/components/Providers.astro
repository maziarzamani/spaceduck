---
const localProviders = [
  { name: "llama.cpp", desc: "Run models directly on your hardware" },
  { name: "LM Studio", desc: "Desktop app with one-click model downloads" },
];

const cloudProviders = [
  { name: "OpenRouter", desc: "Access hundreds of models through one API" },
  { name: "AWS Bedrock", desc: "Enterprise-grade with IAM authentication" },
  { name: "Google Gemini", desc: "Multimodal models with generous free tier" },
];
---

<section class="bg-white px-6 py-20 md:py-28">
  <div class="mx-auto max-w-6xl">
    <div class="mx-auto max-w-2xl text-center">
      <h2 class="text-3xl font-bold tracking-tight text-slate-900 md:text-4xl">
        One gateway, any provider
      </h2>
      <p class="mt-4 text-lg text-text-muted">
        Hot-swap providers at runtime &mdash; mix local chat with cloud
        embeddings, or the other way around.
      </p>
    </div>

    <div class="mt-16 grid gap-8 md:grid-cols-2">
      <div class="rounded-2xl border border-border bg-slate-50 p-8">
        <div class="mb-6 flex items-center gap-3">
          <div
            class="flex h-10 w-10 items-center justify-center rounded-lg bg-emerald-100 text-emerald-600"
          >
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
              ></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-slate-900">Local</h3>
        </div>
        <ul class="space-y-4">
          {
            localProviders.map((p) => (
              <li class="flex items-start gap-3">
                <span class="mt-1.5 h-2 w-2 shrink-0 rounded-full bg-emerald-500" />
                <div>
                  <span class="font-medium text-slate-900">{p.name}</span>
                  <p class="text-sm text-text-muted">{p.desc}</p>
                </div>
              </li>
            ))
          }
        </ul>
      </div>

      <div class="rounded-2xl border border-border bg-slate-50 p-8">
        <div class="mb-6 flex items-center gap-3">
          <div
            class="flex h-10 w-10 items-center justify-center rounded-lg bg-primary/10 text-primary"
          >
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z"
              ></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-slate-900">Cloud</h3>
        </div>
        <ul class="space-y-4">
          {
            cloudProviders.map((p) => (
              <li class="flex items-start gap-3">
                <span class="mt-1.5 h-2 w-2 shrink-0 rounded-full bg-primary" />
                <div>
                  <span class="font-medium text-slate-900">{p.name}</span>
                  <p class="text-sm text-text-muted">{p.desc}</p>
                </div>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </div>
</section>
